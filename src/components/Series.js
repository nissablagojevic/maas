function Series() {
}

Series.defaultProps = {
    borderWidth: 0,
    color: 'rgba(0, 0, 0, 0.50)',
    cursor: 'pointer',
    pointWidth: 10,
    stickyTracking: false,
    width: 10
};

export default Series;
